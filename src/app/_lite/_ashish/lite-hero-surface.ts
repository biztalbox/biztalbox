import type { CSSProperties } from "react";

/** Same background as lite service sections (`bg-[#f2f2f2]`) — hero + layout stay in sync */
export const LITE_HERO_SURFACE_STYLE: CSSProperties = {
  backgroundColor: "#f2f2f2",
};
