import type { CSSProperties } from "react";

/** Same background as lite hero `#section0` — keep header + hero in sync */
export const LITE_HERO_SURFACE_STYLE: CSSProperties = {
  backgroundColor: "#f5f5f5",
};
